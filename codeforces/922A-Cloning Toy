#include<bits/stdc++.h>
using namespace std;

#define ll long long 

int main()
{
	ll o,c,i,j,k;
	cin>>c>>o;
	if(o==0 || (o==1 && c!=0))
	{
		cout<<"No";
		exit(0);
	}
	if((o-1)>c)
	cout<<"No";
	else
	{
		if((c-o+1)%2==0)
		cout<<"Yes";
		else
		cout<<"No";
	}
}
