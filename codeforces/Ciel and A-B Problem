#include <stdio.h>
#include <math.h>
int main(void) {
    int a,b;
    scanf("%d%d",&a,&b);
    int x=a-b;
    int r=x%10;
    if(r>=0 && r<=8){
        printf("%d",x+1);
    }
    else{
        printf("%d",x-1);
    }
	return 0;
}

